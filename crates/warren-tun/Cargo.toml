[package]
name = "warren-tun"
description = "Shim: re-exports the engine's warrenguard-tun-device (privileged per-OS TUN open + applier, experimental-tun). Kept so the SDK's consumers keep the warren_tun path during convergence."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
warrenguard-tun-device = { path = "../../../warrenguard/crates/warrenguard-tun-device" }

[features]
experimental-tun = ["warrenguard-tun-device/experimental-tun"]

[lints]
workspace = true
