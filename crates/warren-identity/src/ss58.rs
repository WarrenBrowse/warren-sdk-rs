//! SS58 codec for the Warren wallet identity public key, re-exported from the
//! canonical `warren-contract` definition so client and backend share one
//! implementation.

pub use warren_contract::ss58::*;
